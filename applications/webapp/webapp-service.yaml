apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  selector:
    app: webapp
  ports:
    - name: webapp
      protocol: TCP
      port: 3000
      targetPort: 80
    - name: wordcloudapi
      protocol: TCP
      port: 3001
      targetPort: 3001
